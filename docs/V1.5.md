# atelier v1.5 — review and requirements

Status: freeze v1 heuristics. Build v1.5 against this document.
If a change is not in here, it is not v1.5.

## Why it degrades

Every night patched a symptom. The brain is 200 lines of if. Each if cancels the last. The shell is two labels.

## P0

- Sitting is not a workplace (spec/file/verify/resume missing as a whole).
- Picker has four policies and no owner.
- atelier pollutes the estate (self-issue #2).

## P1

- Two rooms were emptied, not designed.
- Execute is a checkbox tick or a link out.

## P2

- No sitting memory. Shallow evidence. No fixture gate before Pages.

## v1 honest

A chooser prototype. Not a tracker. Not agentic. Stop calling patches ship.

## v1.5 job

1. Choose work without inventing chores.
2. Sit on spec + file + refuse + done-when.
3. Leave a mark on GitHub that matches the slice.
4. Resume tomorrow from atelier.

## Frozen principles

1. Control plane ≠ work plane. atelier is never hottest unless named.
2. Never open an issue to have something to sit on.
3. Never invent AGENTS.md / README as the slice for finish-one-slice or a named repo.
4. A slice is only: empty checkbox, user-picked issue, or existing file.
5. Picker is a function with fixtures.
6. Two rooms, five surfaces.
7. GitHub is the record.
8. No model on the critical path.

## IA

Desk: empty, order, sitting, resume.
Estate: list+search, repo.
Keys: header mark.

## Picker

Named repo → that repo.
Advance class → work-plane hottest, exclude atelier.
Else tokens.
Else none.

Inside pool: checkbox, else user-picked issue, else existing file, else none.
Vapor issues (Opened by atelier / Build: atelier / slogan title) are not work.

## Sitting must show

Repo, slice, Mark done, spec or file text, tree brief, do/done/do-not, doors last.
File sitting must not create an issue.

## Acceptance

- Finish one slice never returns atelier #2.
- Same sentence returns a real checkbox when one exists.
- Named repo with no issues returns an existing file, not none, not AGENTS.md.
- Nonsense sentence returns none.
- Sit on #N locks to #N.
- Spec or file text on desk before Mark done.
- Mark done refuses when forbid path remains.
- Resume after refresh.
- Estate search finds a repo.
- test/picker.js green before every push.

## Will not do

Another live special-case. Four tabs. Chat. Editor. New repos. WebLLM on this path.

## Order

1.5.0 fixtures → 1.5.1 picker → 1.5.2 sitting → 1.5.3 resume → 1.5.4 estate search.
Do not start 1.5.2 until 1.5.1 is green.
